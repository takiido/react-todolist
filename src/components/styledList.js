import styled from "styled-components";
import {StyledContainer} from "./styledContainer.js";

export const StyledList = styled(StyledContainer)`
    display: grid;
    grid-template-columns: repeat(auto-fill, 240px);
`;